# TGmonitor Frontend Environment Variables

# Backend API URL (default: http://localhost:8000)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Admin password for authentication (must match ADMIN_PASSWORD in backend)
# This is used by the backend to verify login requests
